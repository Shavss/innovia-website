import SectionWrapper from '@/components/ui/SectionWrapper';

export const metadata = {
  title: 'Strategy — Innovia Partners',
};

export default function Strategy() {
  return (
    <main>
      <SectionWrapper background="light">
        <h1>Strategy</h1>
      </SectionWrapper>
    </main>
  );
}
